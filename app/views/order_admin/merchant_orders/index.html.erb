<div class="flex gap-0 items-center justify-between">
  <h2 class="text-xl font-bold text-gray-700">Merchants</h2>
  <div class="hidden lg:block">
    <%= render(BreadcrumbComponent.new) do %>
      <%= render(BreadcrumbItemComponent.new(url: order_admin_dashboard_path, icon: "layout-grid", name: "Dashboard")) %>
      <%= render(BreadcrumbItemComponent.new(name: "Merchant Orders", last: true)) %>
    <% end %>
  </div>
</div>
<%= render("merchants_list", merchants: @merchants, pagy: @pagy, q: @q) %>